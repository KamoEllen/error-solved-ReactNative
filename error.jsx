import React from 'react';
import { Text, View } from 'react-native';

const ErrorMessage = () => {
  return (
    <View>
      <Text>
        This is a very long and verbose error message that extends beyond the
        screen and will not fit within the bounds of the defined width and height
        of the component.This is a very long and verbose error message that extends beyond the
        screen and will not fit within the bounds of the defined width and height
        of the component. .
      </Text>
    </View>
  );
};

export default ErrorMessage;
